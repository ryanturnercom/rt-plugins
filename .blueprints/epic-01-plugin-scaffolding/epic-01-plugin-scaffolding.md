# Epic: Plugin Scaffolding

**Status:** [✓] Completed

## Context

Set up the directory structure, file placeholders, and registry entries for the new documentation agent commands. This is the foundation that all other epics build upon. No code logic — just structure, config examples, and marketplace registration.

## Implementation Overview

Create the `rt-agents/scripts/` and `rt-agents/templates/` directories, add placeholder command files, update `marketplace.json` version, update `config.example.toml` with a `[documentation]` section, and update `README.md` with the new commands.

## Tasks

- [✓] [task-01: Create directory structure and placeholder files](tasks/task-01-create-directory-structure.md)
- [✓] [task-02: Update config.example.toml with documentation section](tasks/task-02-update-config-example.md)
- [✓] [task-03: Update README with new commands](tasks/task-03-update-readme.md)
